# This is a copy of the generated enotest.azure.io_testresources.yaml to provide backwards compatibility
# with old versions of k8s that don't have apiextensions.k8s.io/v1. It needs to be updated manually when
# regenerating its source.
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: testresources.enotest.azure.io
spec:
  group: enotest.azure.io
  version: v1
  names:
    kind: TestResource
    listKind: TestResourceList
    plural: testresources
    singular: testresource
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            values:
              items:
                properties:
                  int:
                    type: integer
                type: object
              type: array
          type: object
        status:
          type: object
      type: object
