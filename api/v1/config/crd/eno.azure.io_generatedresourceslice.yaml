---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: generatedresourceslice.eno.azure.io
spec:
  group: eno.azure.io
  names:
    kind: GeneratedResourceSlice
    listKind: GeneratedResourceSliceList
    plural: generatedresourceslice
    singular: generatedresourceslice
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              generationGeneration:
                description: The product of unfortunate type names and naming conventions.
                  It refers to the metadata.generation property of the Generation
                  resource that caused this resource to be created.
                format: int64
                type: integer
              resources:
                items:
                  properties:
                    manifest:
                      type: string
                    previousManifest:
                      type: string
                    secretName:
                      description: A reference to the secret holding this generated
                        resource. This is only relevant when this resource's kind
                        is Secret.
                      type: string
                  type: object
                type: array
            type: object
          status:
            properties:
              observedGeneration:
                format: int64
                type: integer
              resources:
                description: Elements of resources correspond in index to those in
                  spec.resources at the observed generation.
                items:
                  properties:
                    ready:
                      description: nil if Eno is unable to determine the readiness
                        of this resource. Otherwise it is true when the resource is
                        ready, false otherwise. Like Reconciled, it latches and will
                        never transition from true->false.
                      type: boolean
                    reconciled:
                      description: 'True when the resource has been sync''d to the
                        specified manifest. This property latches: it will remain
                        true if it has ever been true in the life of this generated
                        resource.'
                      type: boolean
                    released:
                      description: True when this representation of the given resource
                        no longer needs to be persisted. When all resources in this
                        slice have been released, the slice can safely be deleted.
                      type: boolean
                    resourceVersion:
                      description: The last seen resource version of the sync'd resource.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
