---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: generatedresourceslice.eno.azure.io
spec:
  group: eno.azure.io
  names:
    kind: GeneratedResourceSlice
    listKind: GeneratedResourceSliceList
    plural: generatedresourceslice
    singular: generatedresourceslice
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              derivedGeneration:
                format: int64
                type: integer
              resources:
                items:
                  properties:
                    manifest:
                      type: string
                  type: object
                type: array
            type: object
          status:
            properties:
              reconciliation:
                items:
                  properties:
                    message:
                      description: Message is a human-readable opaque string that
                        describes the current state.
                      type: string
                    ready:
                      description: Ready is true when the resource has been initialized
                        based on its own semantics. For example, deployments are ready
                        when the expected number of up-to-date pod are passing readiness
                        probes.
                      type: boolean
                    reconciled:
                      description: Reconciled is true when the resource has been reconciled
                        with the current manifest.
                      type: boolean
                    ref:
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
